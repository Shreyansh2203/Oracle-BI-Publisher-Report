ORACLE_USERNAME=your_username@example.com
ORACLE_PASSWORD=your_password
ORACLE_BASE_URL=https://your-instance.fa.ocs.oraclecloud.com

# GitHub auto-save — leave empty to disable
GITHUB_TOKEN=
GITHUB_REPO=

# NOTE: Tunable settings (like timeouts, cache limits, file age thresholds) 
# have safe defaults managed centrally in src/bip_api/config.py.
# You only need to add them here if you wish to override the defaults.
